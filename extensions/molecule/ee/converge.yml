# Molecule converge playbook for execution-environment scenario.
#
# The EE image was already built in the create phase.
# This playbook is a no-op placeholder required by Molecule.

---

- name: "Molecule | Converge (EE) | No-op (build happened in create phase)"
  hosts: "localhost"
  connection: "local"
  gather_facts: false
  tasks:

    - name: "Molecule | Converge (EE) | EE image already built"
      ansible.builtin.debug:
        msg: "Execution Environment image was built in the create phase. See verify phase for validation."
